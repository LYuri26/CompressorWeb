#ifndef UMIDADE_H
#define UMIDADE_H

// Inclui a biblioteca ESPAsyncWebServer.h, que fornece funcionalidades para criar e gerenciar um servidor web assíncrono
#include <ESPAsyncWebServer.h>

// Declaração da função setupUmidadePage
// Esta função configura a página de umidade no servidor web
void setupUmidadePage(AsyncWebServer& server);

#endif // UMIDADE_H
